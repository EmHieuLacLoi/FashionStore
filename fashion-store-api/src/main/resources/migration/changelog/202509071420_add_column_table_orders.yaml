databaseChangeLog:
  - changeSet:
      id: 202509071420
      author: vohieu
      comment: Add column to table orders
      sql: >
        ALTER TABLE `orders`
        ADD COLUMN `shipping_fee` DECIMAL(10,2) NOT NULL DEFAULT 0;